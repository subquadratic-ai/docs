---
title: Raw HTTP requests
description: Call the Aldea API directly without the Deepgram SDK
---

import { Tab, Tabs } from 'fumadocs-ui/components/tabs';

# Raw HTTP requests

<Tabs items={['Python (httpx)', 'Node.js (fetch)', 'Go (net/http)', 'Rust (reqwest)']}>
<Tab value="Python (httpx)">

Call the Aldea API directly using httpx without the Deepgram SDK.

</Tab>
<Tab value="Node.js (fetch)">

Call the Aldea API directly using fetch without the Deepgram SDK.

</Tab>
<Tab value="Go (net/http)">

Call the Aldea API directly using net/http without the Deepgram SDK.

</Tab>
<Tab value="Rust (reqwest)">

Call the Aldea API directly using reqwest without the Deepgram SDK.

</Tab>
</Tabs>
